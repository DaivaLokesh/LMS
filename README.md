First clone the repository 
1. Create the one folder
2. open the terminal and type the command as git init
3. then give -git clone "repo url"
4. Then the proect will get loaded
5. Then open the terminal and cd LMS-> cd lms-backend->type npm i->type node server.js
6. For frontend open another terminal cd LMS->cd my-project->type npm i->type npm run dev
